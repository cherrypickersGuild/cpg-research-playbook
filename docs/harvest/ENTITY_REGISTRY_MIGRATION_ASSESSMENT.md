# Entity registry migration assessment

**Generated by `src/harvest/migrate/entity_assess.py` (Stage 7, checkpoint S7-1) from `state/entity_registry.json`. Do not edit this file by hand — change the implementation and regenerate it.**

- **This assessment migrates 0 entities.** It is a measurement, not a conversion.
- **It creates no taxonomy record.** Not one row here becomes a `record.v1.json` document.
- **It creates no migration bundle and no runtime state.** Nothing is written under `state/taxonomy_harvest/`, `data/harvested/` or `runs/`.
- **The destination taxonomy remains an unresolved product decision.** No destination is chosen, recommended or implied by this document.
- **Stage 7 does not classify entity rows as Product Discovery records**, or as records of any other committed cell.
- The source is one of the 18 protected files. It was **opened read-only** and is byte-identical afterwards.

## 1 · Source and scope

| Property | Value |
|---|---|
| Source path | `state/entity_registry.json` (protected, read-only) |
| Registry `schema_version` | `2` |
| Registry `last_merged_at` | `2026-07-22T03:17:22Z` |
| Top-level keys | `entities`, `last_merged_at`, `metadata`, `schema_version` |
| `metadata` keys | `entity_count_by_entity_type`, `entity_count_by_topic`, `entity_types`, `topics`, `total_entities` |
| Entity rows (derived) | **1,161** |
| Entity rows (declared in `metadata.total_entities`) | 1,161 |
| Expected corpus total | 1,161 |
| Entities migrated | **0** |

Derived, declared and expected totals **agree**: derived == declared · derived == expected · the count is re-derived from the rows on every run, never trusted from `metadata`.

Every row carries exactly these 16 fields; a missing or unrecognised field is a hard error, not a skipped row:

- `conflicting_evidence_log`
- `corroboration_count`
- `description`
- `description_source`
- `discovery`
- `entity_id`
- `entity_key`
- `entity_type`
- `freshness_signal`
- `github_stars`
- `maintainer_or_vendor`
- `name`
- `related_topics`
- `source_url`
- `target_url`
- `topic`

`discovery` is an object with `first_seen_at`, `last_corroborated_at` and `found_via[]` on every row.

## 2 · Population breakdown

### 2.1 · By topic

| Topic | Entities |
|---|---|
| `agent` | 293 |
| `mcp` | 305 |
| `prompt` | 204 |
| `skill` | 359 |
| **Total** | **1,161** |

Reconciles to the population (1,161 == 1,161), and agrees with the registry's own `metadata.entity_count_by_topic`: **yes**.

### 2.2 · By entity type

| Entity type | Entities |
|---|---|
| `benchmark` | 2 |
| `client_sdk` | 24 |
| `dataset` | 16 |
| `framework` | 83 |
| `guide` | 73 |
| `library` | 19 |
| `marketplace` | 56 |
| `other` | 74 |
| `platform` | 95 |
| `product` | 109 |
| `registry` | 7 |
| `server` | 230 |
| `skill` | 259 |
| `spec` | 7 |
| `technique` | 53 |
| `tool` | 54 |
| **Total** | **1,161** |

Reconciles to the population (1,161 == 1,161), and agrees with `metadata.entity_count_by_entity_type`: **yes**.

### 2.3 · Topic × entity type

22 of the 4 × 16 possible pairs are populated. An absent pair is absent from the corpus, not omitted here.

| Topic | Entity type | Entities |
|---|---|---|
| `agent` | `benchmark` | 2 |
| `agent` | `framework` | 66 |
| `agent` | `other` | 20 |
| `agent` | `platform` | 95 |
| `agent` | `product` | 109 |
| `agent` | `tool` | 1 |
| `mcp` | `client_sdk` | 24 |
| `mcp` | `framework` | 17 |
| `mcp` | `other` | 27 |
| `mcp` | `registry` | 7 |
| `mcp` | `server` | 230 |
| `prompt` | `dataset` | 16 |
| `prompt` | `guide` | 54 |
| `prompt` | `library` | 19 |
| `prompt` | `other` | 9 |
| `prompt` | `technique` | 53 |
| `prompt` | `tool` | 53 |
| `skill` | `guide` | 19 |
| `skill` | `marketplace` | 56 |
| `skill` | `other` | 18 |
| `skill` | `skill` | 259 |
| `skill` | `spec` | 7 |
| **Total** |  | **1,161** |

### 2.4 · By schema and description source

The registry carries **one** version field, the top-level `schema_version` (`2`), which applies to all 1,161 rows. There is no per-row schema or record version, so no per-row version dimension is reported — inventing one would be reporting a field the source does not have.

| `description_source` | Entities |
|---|---|
| `snippet-only` | 186 |
| `verified` | 975 |
| **Total** | **1,161** |

`description_source` describes how the **description** was obtained. It is **not** a claim that the URL was fetched, and it cannot supply `verification_status`.

## 3 · Identity and duplicate analysis

| Measurement | Value |
|---|---|
| Rows | 1,161 |
| `entity_id` present (non-blank) | 1,161 |
| `entity_id` null or blank | 0 |
| Distinct `entity_id` | **721** |
| Repeated `entity_id` values | 51 |
| Rows carrying a repeated `entity_id` | 491 |
| Largest repeated group | 23 |
| Repeated groups spanning more than one topic | 49 |
| Repeated groups confined to one topic | 2 |
| Distinct `(topic, entity_id)` | 831 |
| Distinct `entity_key` | **1,161** |
| Blank `entity_key` | 0 |
| Exact duplicate rows (whole-row content match) | 0 |

**`entity_id` cannot be treated as globally unique.** 721 distinct values cover 1,161 rows. Qualifying by topic — the only existing namespace field — raises the distinct count to 831, still short of 1,161, so **topic qualification does not repair uniqueness either**. Only `entity_key` (`topic|normalized(name)`) is unique across the corpus: 1,161 of 1,161.

This is the condition recorded in `docs/entity_id_collision_note.md`: each `1G` extraction invents its own `ent-YYYY-NNNN` sequence with no coordination across topics or runs, and `scripts/merge_entity_registry.sh` deduplicates entirely on `entity_key`, so `entity_id` is carried through as an inert display label. The note reported the collision qualitatively; the measurement above is its current extent.

**Three different things, kept apart:**

- **Exact duplicate source rows** — 0. Mechanically measured by whole-row content.
- **Repeated identifiers over distinct rows** — 491 rows across 51 `entity_id` values. Also mechanically measured, and a different defect with a different fix.
- **Semantically similar entities** — **not measured, and not claimed.** Deciding that two differently-named rows are the same tool needs a reviewed rule that does not exist; asserting it from lexical similarity would manufacture findings.

**No new permanent identifier is selected or proposed here.** That is a product decision and belongs in the checklist in §6.

### 3.1 · Repeated `entity_id` groups

Ordered by group size, then by identifier — never by input order.

| `entity_id` | Rows | Topics | Example `entity_key`s |
|---|---|---|---|
| `ent-2026-0006` | 23 | `agent`, `mcp`, `prompt`, `skill` | `agent|agentada`, `agent|autonomous hr chatbot`, `agent|babyelfagi` … |
| `ent-2026-0007` | 23 | `agent`, `mcp`, `prompt`, `skill` | `agent|babyfoxagi`, `agent|bambooai`, `agent|crewai` … |
| `ent-2026-0001` | 22 | `agent`, `mcp`, `prompt`, `skill` | `agent|adala`, `agent|aider`, `agent|ailice` … |
| `ent-2026-0004` | 22 | `agent`, `mcp`, `prompt`, `skill` | `agent|agentpilot`, `agent|babyagi`, `agent|babycommandagi` … |
| `ent-2026-0002` | 21 | `agent`, `mcp`, `prompt`, `skill` | `agent|agent4rec`, `agent|autogen`, `agent|babybeeagi` … |
| `ent-2026-0003` | 21 | `agent`, `mcp`, `prompt`, `skill` | `agent|agentforge`, `agent|agentic ai foundation (aaif)`, `agent|autogpt` … |
| `ent-2026-0005` | 21 | `agent`, `mcp`, `prompt`, `skill` | `agent|agents (aiwaves-cn)`, `agent|anythingllm`, `agent|babydeeragi` … |
| `ent-2026-0008` | 20 | `agent`, `mcp`, `prompt`, `skill` | `agent|beebot`, `agent|cal.ai`, `agent|devika` … |
| `ent-2026-0009` | 20 | `agent`, `mcp`, `prompt`, `skill` | `agent|agi, inc. (agi-0)`, `agent|blinky`, `agent|chatarena` … |
| `ent-2026-0011` | 20 | `agent`, `mcp`, `prompt`, `skill` | `agent|clippinator`, `agent|cody`, `agent|docsgpt` … |
| `ent-2026-0010` | 19 | `agent`, `mcp`, `prompt`, `skill` | `agent|bumpgen`, `agent|codefuse-chatbot`, `agent|gpt pilot` … |
| `ent-2026-0012` | 19 | `agent`, `mcp`, `prompt`, `skill` | `agent|cody (ajhous44)`, `agent|databerry`, `agent|langbot` … |
| `ent-2026-0014` | 19 | `agent`, `mcp`, `prompt`, `skill` | `agent|agentverse`, `agent|dev-gpt`, `agent|devopsgpt` … |
| `ent-2026-0013` | 18 | `agent`, `mcp`, `prompt`, `skill` | `agent|agentgpt`, `agent|demogpt`, `agent|devon` … |
| `ent-2026-0015` | 17 | `agent`, `mcp`, `prompt`, `skill` | `agent|automata`, `agent|english compiler`, `agent|langchain` … |
| `ent-2026-0016` | 16 | `agent`, `mcp`, `prompt`, `skill` | `agent|ai legion`, `agent|fastagency`, `agent|godmode` … |
| `ent-2026-0017` | 15 | `agent`, `mcp`, `prompt`, `skill` | `agent|agent swarm`, `agent|autopr`, `agent|friday` … |
| `ent-2026-0018` | 13 | `agent`, `mcp`, `prompt`, `skill` | `agent|bloop`, `agent|genia`, `agent|ix` … |
| `ent-2026-0019` | 13 | `agent`, `mcp`, `prompt`, `skill` | `agent|bondai`, `agent|gptdiscord`, `agent|lemon agent` … |
| `ent-2026-0020` | 13 | `agent`, `mcp`, `prompt`, `skill` | `agent|astrbot`, `agent|chemcrow`, `agent|gpt-migrate` … |
| `ent-2026-0021` | 11 | `agent`, `mcp`, `prompt`, `skill` | `agent|adept ai`, `agent|data-to-paper`, `agent|gpt runner` … |
| `ent-2026-0022` | 11 | `agent`, `mcp`, `prompt`, `skill` | `agent|aiaw (ai as workspace)`, `agent|bardeen`, `agent|eidolon` … |
| `ent-2026-0023` | 11 | `agent`, `mcp`, `prompt`, `skill` | `agent|codegen`, `agent|evo.ninja`, `agent|loop gpt` … |
| `ent-2026-0024` | 10 | `agent`, `mcp`, `prompt`, `skill` | `agent|devin`, `agent|flowise`, `agent|l2mac` … |
| `ent-2026-0025` | 8 | `agent`, `mcp`, `prompt` | `agent|dosu`, `agent|gpt researcher`, `agent|mentat` … |
| `ent-2026-0026` | 3 | `agent`, `mcp`, `prompt` | `agent|mini agi`, `mcp|dicomweb mcp server`, `prompt|langsmith` |
| `ent-2026-0027` | 3 | `agent`, `mcp`, `prompt` | `agent|multiagent debate`, `mcp|opdstar nhi mcp`, `prompt|phoenix` |
| `ent-2026-0028` | 3 | `agent`, `mcp`, `prompt` | `agent|multi-gpt`, `mcp|pubmed search`, `prompt|trulens` |
| `ent-2026-0029` | 3 | `agent`, `mcp`, `prompt` | `agent|mutahunter`, `mcp|wellness cgm mcp`, `prompt|inspect ai` |
| `ent-2026-0030` | 3 | `agent`, `mcp`, `prompt` | `agent|nlsom`, `mcp|genomic agent discovery`, `prompt|opik` |
| `ent-2026-0031` | 3 | `agent`, `mcp`, `prompt` | `agent|openagents`, `mcp|safari mcp`, `prompt|evalview` |
| `ent-2026-0032` | 3 | `agent`, `mcp`, `prompt` | `agent|openagi`, `mcp|firefox devtools mcp`, `prompt|promptext` |
| `ent-2026-0033` | 3 | `mcp`, `prompt`, `skill` | `mcp|selenium mcp server`, `prompt|pezzo`, `skill|openpaw` |
| `ent-2026-0034` | 3 | `agent`, `mcp`, `skill` | `agent|privategpt`, `mcp|comet mcp`, `skill|think better` |
| `ent-2026-0035` | 3 | `agent`, `mcp`, `skill` | `agent|react agent`, `mcp|ashra mcp`, `skill|claude-code-guide` |
| `ent-2026-0036` | 3 | `agent`, `mcp`, `skill` | `agent|self-operating computer`, `mcp|yutu`, `skill|claude-howto` |
| `ent-2026-0037` | 3 | `agent`, `mcp`, `skill` | `agent|smol developer`, `mcp|auto browser`, `skill|anthropics/skills` |
| `ent-2026-0038` | 3 | `agent`, `mcp` | `agent|claude-code-action`, `agent|stackwise`, `mcp|browser devtools mcp` |
| `ent-2026-0039` | 2 | `agent` | `agent|claude-code-security-review`, `agent|onereach.ai` |
| `ent-2026-0040` | 2 | `mcp`, `skill` | `mcp|medialyst`, `skill|claude-plugins-official` |
| `ent-2026-2001` | 2 | `mcp` | `mcp|figma mcp server`, `mcp|mcp-server-templates` |
| `ent-2026-2002` | 2 | `mcp`, `skill` | `mcp|ncp`, `skill|figma-implement-design` |
| `ent-2026-2003` | 2 | `mcp`, `skill` | `mcp|mcpx`, `skill|skill authoring best practices` |
| `ent-2026-2004` | 2 | `agent`, `skill` | `agent|mcgravity`, `skill|skills for enterprise` |
| `ent-2026-2005` | 2 | `mcp`, `skill` | `mcp|open-mcp`, `skill|openai/plugins` |
| `ent-2026-2006` | 2 | `mcp`, `skill` | `mcp|waystation`, `skill|vercel-labs/skills` |
| `ent-2026-2007` | 2 | `mcp`, `skill` | `mcp|mcp-access-point`, `skill|huggingface/skills` |
| `ent-2026-2008` | 2 | `mcp`, `skill` | `mcp|pmcp`, `skill|trailofbits/skills` |
| `ent-2026-2009` | 2 | `mcp`, `skill` | `mcp|biomcp`, `skill|expo/skills` |
| `ent-2026-2010` | 2 | `mcp`, `skill` | `mcp|medical-mcp`, `skill|remotion-dev/skills (@remotion/skills)` |
| `ent-2026-2011` | 2 | `mcp`, `skill` | `mcp|fhir-mcp-server`, `skill|apollographql/skills` |

### 3.2 · URLs, where a taxonomy identity would have to come from

| Measurement | Value |
|---|---|
| Rows with an absolute http(s) `target_url` | **1,024** |
| Rows whose `target_url` is unusable | **137** |
| … of which carry the `"unknown"` sentinel | 137 |
| Rows with an absolute http(s) `source_url` | 1,103 |
| Rows whose `source_url` is unusable | 58 |
| … of which carry the `"unknown"` sentinel | 0 |
| Rows where `target_url` == `source_url` | 319 |
| Distinct usable `target_url` | **983** |
| Usable `target_url` values shared by more than one row | 36 |
| Rows involved in a shared `target_url` | 77 |
| Distinct hosts among usable `target_url` | 231 |

Two consequences follow directly, and both are structural rather than a matter of effort: **137 rows have no URL to derive an identity from**, and **77 rows share a URL with another row**, so URL-derived identity would merge entities the registry treats as distinct.

## 4 · Field-mapping assessment

### 4.1 · The 16 source fields, classified

**Safely reusable as a generic taxonomy field**

| Field | Assessment |
|---|---|
| `description` | Maps cleanly onto `summary`. |
| `name` | Maps cleanly onto `title`. |
| `source_url` | Would be `source_url`, the surfacing page; frequently identical to `target_url`. |
| `target_url` | Would be `target_url` — but only where it is an absolute http(s) URL (see §3 and §4.1). |

**Potentially useful as a domain-specific field**

| Field | Assessment |
|---|---|
| `corroboration_count` | How many discovery hits agreed. Evidence about discovery, not about the page. |
| `entity_type` | Sixteen values with no committed vocabulary behind them. Usable as a domain field; not a `primary_category`. |
| `github_stars` | Popularity metric with no home in the generic contract. |
| `maintainer_or_vendor` | Nearest generic field is `publisher`, but the two are not the same claim: a maintainer is not who published the page. |
| `related_topics` | Harvest-lane cross-references, not taxonomy topics. |

**Retainable only as provenance / raw data**

| Field | Assessment |
|---|---|
| `conflicting_evidence_log` | Retained verbatim; it has no generic counterpart. |
| `description_source` | `verified`/`snippet-only` describes how the DESCRIPTION was obtained, not whether the URL was fetched. It cannot supply `verification_status`. |
| `discovery` | `first_seen_at` could supply `discovered_at`; `found_via[]` is discovery metadata and is NEVER a URL. |
| `entity_id` | Not unique (see §3). Retainable only as a `legacy_ids[]` label. |
| `entity_key` | The registry's real dedup key (`topic|normalized(name)`). A legacy identifier, not a taxonomy identity. |
| `freshness_signal` | Free-text prose ('© 2026 footer', promotional copy). Not a date, and not convertible into one. |

**Unsafe or ambiguous without a product decision**

| Field | Assessment |
|---|---|
| `topic` | A HARVEST-LANE label (`agent`/`mcp`/`prompt`/`skill`), not one of the three committed taxonomy topics. Reusing the name would silently redefine `topic`. |

A field is classified by what it can *honestly* support, not by name similarity. `maintainer_or_vendor` and `publisher` are the clearest example: they look interchangeable and are not the same claim.

### 4.2 · Required record contracts the registry cannot meet

| Contract | What `record.v1.json` requires | What the registry supports | Verdict |
|---|---|---|---|
| Target / identity URL | `target_url` must be an absolute http(s) URL; `identity_url`, `record_id` and `content_id` are all derived from it. | 1,024 of 1,161 rows carry a usable absolute URL; 137 carry the `"unknown"` sentinel. | BLOCKING for 137 rows — no URL means no identity, and identity cannot be invented. |
| Identity collision | One `identity_url` is one record; two rows sharing a URL are one record, not two. | 36 usable URLs are shared by 77 rows; 983 distinct usable URLs back 1,024 rows. | BLOCKING — URL-derived identity would silently merge entities the registry treats as distinct. |
| Topic and primary category | `topic` and `primary_category` must come from the committed 12-cell taxonomy. | The registry's four `topic` values are harvest lanes, and its sixteen `entity_type` values have no committed category behind them. | BLOCKING — no committed cell describes 'an MCP server' or 'a skill'. |
| Classification evidence | `classification` requires a `rule_id`, a rationale and quoted evidence. | No rule fired: the lane label was assigned by the harvester, not by a precedence rule over the page. | Requires a decision — a migration-specific rule id would have to be defined, as D7-E does for AX cases. |
| Verification / access evidence | `access_status`, `http_status`, `verification_status`, `content_hash`, `last_checked_at`. | `description_source` describes the description, not the page; no fetch is recorded anywhere in a row. | Honest values exist (`not_checked` / `unverified`); no page-level claim can be made. |
| Required scores | `relevance_score`, `quality_score`, `audience_fit_score`, `freshness_score` are required keys. | Nothing in the registry was scored by the committed scorers. | Null is the only honest value, exactly as D7-G decides for AX cases. |
| Dates | `published_at` and `updated_at` must be ISO-8601 UTC or null. | `discovery.first_seen_at` is a discovery date, not a publication date; `freshness_signal` is prose. | `published_at` would be null for every row; `discovered_at` is available. |
| Provenance | `provenance.source_id`, `source_adapter`, `raw`, and `discovered_via[]` as non-URL metadata. | The complete row can be retained under `provenance.raw`; `found_via[]` is already `{hit_id, platform}`-shaped, with 40 empty objects. | Supported — provenance is the one contract the registry meets comfortably. |
| Facet applicability | `case_facets` is required in `cases__domain-applications`, forbidden under `research-and-models` and `discourse`. | Entities are tools and products, not deployment case studies; no industry, business function or use case is recorded. | Not applicable under any plausible destination — and inferring facets is forbidden. |

**No record mapping is manufactured here.** A destination field with a similar name is not evidence that the source can fill it.

## 5 · Candidate destinations

Four approaches are described. **None is approved, recommended or chosen** — each is recorded with what it buys, what it breaks, and what still has to be decided.

### A · A separate entity taxonomy and entity record schema

*Benefits*

- Entities keep their own identity rule (`entity_key`), so nothing is merged by URL and the 137 URL-less rows remain representable.
- No committed contract is bent to fit an object it was not designed for.

*Contract conflicts*

- A second schema, a second validator and a second publication path to maintain.
- Every consumer must learn which of two record shapes it is holding.

*Decisions still required*

- Whether entities are published at all, or only held internally.
- Who owns the entity vocabulary, and what its categories are.

### B · Selective conversion into existing content-record categories

*Benefits*

- Reuses the committed record contract, validator, artifacts and gate with no new schema.
- A curated subset — the 1,024 rows with usable URLs — could migrate first.

*Contract conflicts*

- No committed cell describes a tool or a server; `topic` and `primary_category` would have to be invented.
- URL-derived identity merges the 77 rows that share 36 URLs.
- 137 rows are unrepresentable at all.

*Decisions still required*

- Which cells, if any, may receive entity rows.
- What happens to rows that cannot be represented — dropped, held, or converted differently.

### C · Entities as linked reference data, not published records

*Benefits*

- No identity, category or evidence claim is manufactured; the registry keeps its own shape.
- Records could reference an entity without the entity becoming a record.

*Contract conflicts*

- Needs a reference contract that does not exist yet.
- Provides no answer for entities that a reader would expect to see published.

*Decisions still required*

- Whether any consumer actually needs entities as first-class records.

### D · Retain the registry unchanged until a product decision is made

*Benefits*

- Costs nothing, risks nothing and loses nothing; the registry is protected and stable.
- Keeps the 491 rows with repeated `entity_id`s from being frozen into a taxonomy identity.

*Contract conflicts*

- The corpus stays outside the taxonomy pipeline and gains none of its guarantees.

*Decisions still required*

- Whether the taxonomy is the intended destination at all.

## 6 · Migration risks and required decisions

**Identity stability.** `entity_id` is not a key: 721 distinct values cover 1,161 rows, and 51 of them are reused by 491 rows. Qualifying by topic helps but does not fix it (831 distinct topic-qualified ids). Only `entity_key` is unique (1,161/1,161), and it is a merge key invented by `merge_entity_registry.sh`, not a stable public identifier.

**Duplicate handling.** There are **no exact duplicate rows** (0), so every repeat is a repeated IDENTIFIER over distinct content — a different failure with a different fix. Semantic near-duplicates are not measured here and must not be claimed without a reviewed rule.

**Topic and category semantics.** The four registry topics are harvest lanes. Mapping them onto taxonomy topics would redefine `topic` for every existing record, and no committed category describes an MCP server, a skill or a prompt technique.

**Missing URL and evidence ownership.** 137 rows carry `target_url: "unknown"` and 58 carry an unusable `source_url`. A record cannot be built without an absolute URL, and no row records that any page was ever fetched.

**Compatibility with existing harvested records.** Entity rows would enter the same identity space as harvested records. Any URL an entity shares with a harvested record becomes one identity with two provenance stories.

**Promotion and merge collision risk.** 36 usable URLs are already shared inside the registry itself (77 rows). A merge keyed on identity would collapse them silently.

**Provenance preservation.** Every row can be retained verbatim under `provenance.raw`, and 1,247 `found_via` items are already non-URL discovery metadata — but 40 of them are empty objects, a schema drift that a migration would have to decide about rather than normalise away.

**Rollback and regenerability.** The registry is protected and is never written by this pipeline, so any migration is regenerable from it and rollback is deleting the output. That property is worth keeping: it is what makes a migration safe to retry.

**Object-type heterogeneity.** The corpus is mixed: 16 `entity_type` values spanning products, platforms, servers, skills, specs, guides, datasets and techniques. These are not one kind of thing, and one destination contract may not fit all of them.

### 6.1 · Supporting measurements

| Measurement | Value |
|---|---|
| Rows with a `github_stars` value | 245 |
| Rows with `github_stars: null` | 916 |
| Rows with a non-empty `related_topics` | 365 |
| Rows with a non-empty `conflicting_evidence_log` | 26 |
| `found_via` items in total | 1,247 |
| Rows with an empty `found_via` | 0 |
| Earliest `discovery.first_seen_at` | `2026-07-06` |
| Latest `discovery.first_seen_at` | `2026-07-15` |
| Distinct `discovery.first_seen_at` | 7 |
| Latest `discovery.last_corroborated_at` | `2026-07-15` |
| Rows with a blank `description` | 0 |
| Rows with a blank `maintainer_or_vendor` | 0 |
| Rows with a blank `freshness_signal` | 0 |

`found_via` item shapes, which is where the drift shows:

| Item keys | Occurrences |
|---|---|
| *(empty object)* | 40 |
| `hit_id`, `platform` | 1,207 |

`related_topics` values, which are harvest lanes and not taxonomy topics:

| Related topic | Occurrences |
|---|---|
| `agent` | 285 |
| `mcp` | 71 |
| `prompt` | 13 |
| `skill` | 21 |

`corroboration_count` distribution:

| `corroboration_count` | Entities |
|---|---|
| 1 | 1,108 |
| 2 | 30 |
| 3 | 16 |
| 4 | 4 |
| 5 | 3 |
| **Total** | **1,161** |

### 6.2 · Follow-up decision checklist

Decisions, not implementation steps. Each has to be answered by a person before any entity migration checkpoint can be written.

1. Decide whether entities belong in the taxonomy at all, or remain reference data.
2. Decide what a stable entity identifier is, given that `entity_id` is not one and `entity_key` is a merge artefact.
3. Decide what happens to the rows with no usable URL — they cannot be identified by URL.
4. Decide whether two rows sharing a URL are one thing or two.
5. Decide the destination vocabulary: taxonomy topics and categories, a new entity vocabulary, or neither.
6. Decide whether an entity record may exist with no page-level evidence and null scores.
7. Decide the `found_via` empty-object drift: fix at source, or accept and record it.
8. Decide who reviews the result, and against what acceptance criterion.

Until every one of them is answered, **the destination taxonomy remains an unresolved product decision and 0 entities are migrated.**
